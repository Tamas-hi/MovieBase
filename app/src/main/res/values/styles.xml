<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/blackTest</item>
        <item name="colorPrimaryDark">@color/blackTest</item>
        <item name="colorAccent">@color/blackTest</item>
    </style>

</resources>
